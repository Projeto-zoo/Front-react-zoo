// index.tsx (dentro da pasta cadastro)
import React from 'react';
import Login from '../../components/login/login';

const Cadastro: React.FC = () => {
  return <Login />;
};

export default Cadastro;
